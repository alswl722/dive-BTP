## 프로젝트 개요

**대회** : DIVE 2026 (부산 글로벌 데이터 해커톤), 부산테크노파크(BTP) × 한국평가데이터(KODATA) 발제 트랙
**팀** : FoeDev (4인)
**주최/주관** : 부산광역시 / (재)부산테크노파크

## 해결하려는 문제

부산TP 발제 브리핑("기업 선정·심사 과정의 실무적 한계") 기준으로 두 축:

1. **정보 비대칭성** — 담당자가 신청기업 제출 서류만으로 우수 기업을 가려내야 하고, 서류상 내용과 실제 역량(재무/신용/고용)을 검증할 객관적 지표 도구가 없음
2. **반복지원 및 중복성 검증 한계** — 매년 약 200개 사업, 1,200여개 기업 지원. 부서별 심사가 분절적으로 이루어져 특정 기업의 중복 지원을 즉각 파악하기 어려움. 일부 기업의 반복 선정 경향 존재

## 핵심 기능 (범위 확정)

**포함 (S-tier, 필수)**

- 기업 종합 스코어카드: 재무 5개년 추세, 부채비율, 인증뱃지(벤처/이노비즈/연구소), 특허, 업종 평균 대비 백분위, 사업목적-지원사업 정합성 체크, NTIS 연구이력, 지원사업 이력 타임라인(선정+탈락+포기)
- 중복지원 탐지: 규칙기반(`GROUP BY 기업일련번호 HAVING COUNT(*) >= N`), 반복선정 랭킹(건수/금액 분리)
- 기업 비교 화면: 2개 이상 기업 나란히 비교, 경과기간/선정률/사업유형 분포 포함
- 전사관리자 총괄 대시보드: 부서 무관 통합 지표, 사업유형별 집계, 연도별 추이, 데이터 품질 경고

**로드맵으로만 언급 (발표자료에 한 줄)**

- 엔티티 매칭/중복탐지(사업자번호 표기 차이 대응)
- 계열사 구조 분석(금융위 기업기본정보 API 연동 시)

## 기술 스택

- 백엔드: FastAPI (Python)
- 프론트엔드: Next.js
- DB: PostgreSQL
- 배포/실행: Docker Compose

## 데이터 소스

### KODATA 기업데이터 (5개 시트, `기업일련번호` 기준 조인)

1. 기업정보 — 기본정보/상태/상태·고용지표(2020~2024)/재무지표(2020~2024)/인증보유여부/특허누적/연구개발조직
2. 특허및실용신안
   3-1. NTIS(주관) — 정부 R&D 과제 수행 이력
   3-2. NTIS(위탁) — 공동연구 이력
3. 법인사업목적 — 등기부등본 사업목적 텍스트(최대 10줄)

### 부산TP 사업기업목록 (6개 시트, `기업일련번호` 기준 조인)

- 2022~2024 사업목록 (사업 메타정보)
- 2022~2024 기업지원목록 (실제 선정 기록: 코드/사업유형/지원구분/지원품목/선정일/선정결과/지원금/기업일련번호/업종코드)
- 참고: 사업구분참조, 기업구분참조 (카테고리 체계)

### ⚠️ 알려진 데이터 이슈 (처리 필수)

- **단위 불일치** : `1인평균 연간급여`만 단위가 "원", 나머지 재무지표(매출액/자산총계 등)는 "천원". 정규화 전 반드시 스케일 통일 (1000배 오차 주의)
- **업종코드 포맷 연도별 상이** : 2022년 `42500`(5자리 숫자) / 2023~2024년 `C29199`, `C29299`(알파벳+숫자). 그룹핑 전 정규화 필요
- **결측치** : 지원금/시작일/종료일/업종코드/주생산품/설립연도 등에 결측 존재. null 처리 정책 사전 합의 필요
- **표본 특성** : 샘플 데이터의 기업 11개 전원이 2회 이상 반복 선정됨(최대 21회) — 발제사가 의도적으로 중복 사례 위주로 큐레이션했을 가능성. 실제 발제 데이터는 이 분포와 다를 수 있음

## 개발 원칙

1. **규칙기반 우선** — ML/AI가 필요한 지점인지 먼저 판단. 명확한 규칙으로 100% 해결되는 문제(중복탐지 등)에 모델을 얹지 않는다
2. **사용자 페르소나 고정** — 이 도구의 사용자는 "부산TP 심사 담당자/관리자"다. 기업 대상 기능을 섞지 않는다
3. **ETL은 설정파일 기반** — 실제 발제 데이터가 샘플과 컬럼명/포맷이 다를 수 있으므로, 컬럼 매핑을 하드코딩하지 말고 config로 분리
4. **권한은 필터로, 뷰는 하나로** — 부서별 화면을 따로 만들지 않는다. 담당자/관리자 role에 따라 동일 컴포넌트에 쿼리 필터만 다르게 적용

## 폴더 구조

```
foedev-project/
├── docker-compose.yml
├── frontend/          (Next.js)
├── backend/
│   ├── etl/           (xlsx → PostgreSQL 적재, config 기반 컬럼 매핑)
│   └── app/           (FastAPI 라우터/스키마/서비스 로직)
└── db/
    └── init.sql
```

## 참고

- 원본 문제정의 슬라이드: "기업 선정·심사 과정의 실무적 한계" (부산TP-KODATA 발제 브리핑)
